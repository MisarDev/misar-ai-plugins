---
description: "SEO content generation — Research, outline, write, humanize, optimize, score. Topic to publish-ready article."
argument-hint: "[agents] [--topic=keyword] [--type=blog] [--keywords=k1,k2]"
---

Launch the **seo-content-agents** agent to generate publish-ready SEO content.

1. Parse arguments: agents (`research`, `outline`, `write`, `humanize`, `optimize`, `score`), topic (`--topic=`), type (`--type=`), keywords (`--keywords=`)
2. Launch `seo-content-agents` with parsed parameters
3. The agent handles research, content architecture, writing, humanization, SEO optimization, and quality scoring.
