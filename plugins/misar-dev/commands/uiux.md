---
description: "UI/UX design audit — Spacing, Typography, Components, Accessibility, Performance, Mobile, Animation, Dark Mode."
argument-hint: "[agents] [--path=src/]"
---

Launch the **uiux-agents** agent to perform a comprehensive UI/UX design quality audit.

1. Parse arguments: agents (`spacing`, `typography`, `components`, `accessibility`, `performance`, `mobile`, `animation`, `dark-mode`), path (`--path=`)
2. Launch `uiux-agents` with parsed parameters
3. The agent handles component discovery, WCAG validation, spacing grid verification, responsive analysis, and reporting.
