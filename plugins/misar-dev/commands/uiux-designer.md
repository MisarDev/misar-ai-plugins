---
description: "UI/UX design consultation — Project analysis, design system, brand identity, component specs, design critique."
argument-hint: "[agents] [--platform=web] [--component=button]"
---

Launch the **uiux-designer-agents** agent to perform design consultation.

1. Parse arguments: agents (`analyze`, `guidelines`, `brand`, `component`, `critique`), platform (`--platform=`), component (`--component=`)
2. Launch `uiux-designer-agents` with parsed parameters
3. The agent handles platform detection, design system creation, brand recommendations, component specs, and critique.
